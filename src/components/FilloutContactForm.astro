---
/**
 * CONTACT FORM EMBED (lazy-loaded)
 * The Fillout script loads only when the visitor scrolls near the form or
 * clicks "Start Form" — it never blocks initial page load.
 * To swap in a new embed, update the formId below.
 */
import LazyFilloutEmbed from "./LazyFilloutEmbed.astro";
---
<LazyFilloutEmbed formId="71p9BkecAGus" heading="Contact Form" />
