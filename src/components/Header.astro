---
import FilloutEstimateButton from "./FilloutEstimateButton.astro";
import { navItems, site } from "../data/site";
---
<header class="site-header">
  <div class="topbar">
    <div class="container topbar-inner">
      <a href={`mailto:${site.email}`}>{site.email}</a>
      <span>{site.address}</span>
      <a href={`tel:${site.tel}`}>Call {site.phoneDisplay}</a>
    </div>
  </div>
  <nav class="nav container" aria-label="Primary navigation">
    <a class="logo" href="/" aria-label="VMG Painting & Remodeling home">VMG Painting & Remodeling</a>
    <input id="menu-toggle" class="menu-toggle" type="checkbox" aria-label="Toggle navigation" />
    <label class="menu-button" for="menu-toggle">Menu</label>
    <div class="nav-links">
      {navItems.map((item) => <a href={item.href}>{item.label}</a>)}
      <FilloutEstimateButton className="nav-cta-wrap" />
    </div>
  </nav>
</header>

<style>
  .site-header { position: sticky; top: 0; z-index: 50; background: var(--white); box-shadow: 0 8px 24px rgba(36,32,24,.08); }
  .topbar { background: var(--charcoal); color: rgba(255,255,255,.82); font-size: .86rem; }
  .topbar-inner { min-height: 38px; display: flex; gap: 22px; align-items: center; justify-content: space-between; }
  .nav { min-height: 76px; display: flex; align-items: center; justify-content: space-between; gap: 24px; }
  .logo { font-size: 1.25rem; font-weight: 900; color: var(--ink); max-width: 300px; line-height: 1.15; }
  .nav-links { display: flex; align-items: center; gap: 22px; font-weight: 800; font-size: .95rem; }
  .nav-links a:hover, .nav-links a:focus-visible { color: var(--gold-dark); }
  .nav-cta-wrap { align-items: center; }
  .menu-toggle, .menu-button { display: none; }
  @media (max-width: 920px) {
    .topbar { display: none; }
    .menu-button { display: inline-flex; background: var(--gold); border-radius: 999px; padding: 10px 16px; font-weight: 900; }
    .menu-toggle { position: absolute; opacity: 0; }
    .nav { flex-wrap: wrap; min-height: 68px; }
    .nav-links { display: none; width: 100%; flex-direction: column; align-items: stretch; padding: 0 0 18px; }
    .menu-toggle:checked ~ .nav-links { display: flex; }
    .nav-links a { padding: 8px 0; }
    .nav-cta-wrap { width: 100%; justify-content: center; }
  }
</style>
