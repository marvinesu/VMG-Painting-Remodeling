<!--
  CHATBOT PLACEHOLDER
  Paste the chatbot provider's embed script/container code inside the div below
  when it is provided. Most chat widgets render their own floating launcher, so
  no extra styling should be needed. If the widget overlaps the floating call
  button (bottom-right), adjust the widget's offset in its provider settings.
-->
<div id="chatbot-placeholder" class="chatbot-placeholder" aria-hidden="true">
  <!-- Chatbot embed code goes here. -->
</div>
<style>
  .chatbot-placeholder { position: fixed; right: 18px; bottom: 76px; z-index: 55; width: 1px; height: 1px; overflow: hidden; }
</style>
