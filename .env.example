# SMTP configuration for lead notification emails (Nodemailer).
# Set these in the Hostinger panel (or a local .env for testing).
# The forms return a friendly "temporarily unavailable" message until these are set.
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
LEAD_NOTIFY_EMAIL=vmgpaintingnremodelingllc@gmail.com

# Optional analytics placeholder. Paste provider IDs in the layout only after approval.
PUBLIC_SITE_URL=https://vmgpaintingnremodelingllc.com
